# Dependencias de producción.
# Nota: pytest y httpx se incluyen aquí (y no solo en requirements-dev.txt)
# porque el entorno de despliegue objetivo también corre la suite de tests
# como parte del pipeline de arranque/CI de la asignatura.
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0
clipspy>=1.0.1
python-dotenv>=1.0,<2.0
pytest>=8.0,<9.0
httpx>=0.27,<1.0
