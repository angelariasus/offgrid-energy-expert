version: "3.9"

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped

  # Servicio de ejemplo para cuando se desarrolle el frontend Next.js/React
  # (fuera del alcance de este entregable). Descomentar y ajustar la ruta de
  # build una vez que exista el proyecto de frontend.
  # frontend:
  #   build:
  #     context: ../offgrid-energy-expert-frontend
  #     dockerfile: Dockerfile
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     - NEXT_PUBLIC_API_URL=http://backend:8000/api
  #   depends_on:
  #     - backend
